{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "cheerio": "^1.0.0-rc.12",
    "nuxt": "3.0.0-rc.11",
    "stylus": "^0.59.0"
  },
  "dependencies": {
    "axios": "^1.1.2",
    "normalize.css": "^8.0.1"
  }
}
